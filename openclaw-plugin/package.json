{
    "name": "@hapi/openclaw-plugin",
    "private": true,
    "version": "0.1.0",
    "type": "module",
    "scripts": {
        "start": "bun run src/index.ts",
        "dev": "bun --watch run src/index.ts",
        "test": "bun test",
        "typecheck": "tsc --noEmit",
        "build": "bun build src/index.ts --outdir dist --target bun"
    },
    "dependencies": {
        "@hapi/protocol": "workspace:*",
        "hono": "^4.11.2"
    },
    "devDependencies": {
        "@types/node": ">=25",
        "bun-types": "^1.3.5",
        "typescript": "^5"
    }
}
